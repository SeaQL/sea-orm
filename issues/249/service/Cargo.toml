[package]
name = "service"
version = "0.1.0"
edition = "2024"
rust-version = "1.85.0"
publish = false

[dependencies]
sea-orm = { path = "../../../", default-features = false }

[dev-dependencies]
sea-orm = { path = "../../../", features = ["mock"] }
